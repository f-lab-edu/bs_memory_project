import { describe, test } from 'vitest';

describe('Card test', () => {
  test('renders theme, address, contents with props.data', () => {});
  test('renders transparent theme, address, contents according to current hide option', () => {});
  test('renders bible scripts according to current bible version', () => {});
});
