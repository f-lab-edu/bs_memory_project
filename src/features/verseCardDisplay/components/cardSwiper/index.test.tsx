import { describe, test } from 'vitest';

describe('CardSwiper rendering test', () => {
  test('renders "끝 구절로","처음 구절로","이전 구절로","다음 구절로" nav buttons', () => {});
  test('renders cards with props.data and the first card is presenting', () => {});
  test('renders paging buttons of cards', () => {});
});

describe('CardSwiper operation test', () => {
  test('presents the next card when clicks "다음 구절로" nav button', () => {});
  test('presents the previous card when clicks "이전 구절로 nav button"', () => {});
  test('presents the first card when clicks "처음 구절로" nav button', () => {});
  test('presents the last card when clicks "끝 구절로" nav button', () => {});
  test('presents the referencing card when clicks paging button', () => {});
});
