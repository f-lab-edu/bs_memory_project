import { describe, test } from 'vitest';

describe('Timer operation test', () => {
  test('renders left time with "mm:ss" format which decreases per seconds', () => {});
  test('when left time becomes "00:00", "시험시간이 종료되었습니다." message pops up', () => {});
  test('when clicks "일시정지" button, text changes to "다시시작" and left time preserves', () => {});
  test('when clicks "다시시작" button, text changes to "일시정지" and the left time which has been preserved starts decreasing again', () => {});
});
