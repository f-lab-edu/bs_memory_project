import { describe, test } from 'vitest';

describe('DrillingPage Test', () => {
  test('if no selected verses on page reload, it redirects to "/" ');
  test('renders "홈으로" and "시험보기" links, "성경버전" and "숨김" options, and card slide', () => {});
});
